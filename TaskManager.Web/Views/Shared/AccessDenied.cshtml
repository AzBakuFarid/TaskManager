@{ 
     ViewData["Title"] = "Not allowed";
     string errorMessage = ViewData["ErrorMessage"]?.ToString() ?? "The last action that was tried is not allowed";
}﻿
    <div class=" container text-center background-image-css" style="background-image:url('/images/403.png');">
        <h3 class="text-danger">@errorMessage</h3>
    </div>
